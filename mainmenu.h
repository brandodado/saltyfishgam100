#pragma once

// Declares the function from utils.c so other files can use it
/* <-- FIX: Removed IsAreaClicked from here --> */

void Main_Menu_Init(void);
void Main_Menu_Update(void);
void Main_Menu_Exit(void);